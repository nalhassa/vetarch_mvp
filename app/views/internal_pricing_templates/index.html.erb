<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All internal pricings
    </h1>

    <a href="/internal_pricings/new" class="btn btn-block btn-outline-secondary">
      Add a new internal pricing
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Cet
        </th>

        <th>
          Price
        </th>

        <th>
          Cogs
        </th>

        <th>
          Medication
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @internal_pricings.each do |internal_pricing| %>
      <tr>
        <!-- Display internal_pricing.id start -->
        <td>
          <%= internal_pricing.id %>
        </td>
        <!-- Display internal_pricing.id end -->

        <!-- Display internal_pricing.cet start -->
        <td>
          <%= internal_pricing.cet %>
        </td>
        <!-- Display internal_pricing.cet end -->

        <!-- Display internal_pricing.price start -->
        <td>
          <%= internal_pricing.price %>
        </td>
        <!-- Display internal_pricing.price end -->

        <!-- Display internal_pricing.cogs start -->
        <td>
          <%= internal_pricing.cogs %>
        </td>
        <!-- Display internal_pricing.cogs end -->

        <!-- Display internal_pricing.medication_id start -->
        <td>
          <% if internal_pricing.medication.present? %>
            <a href="/medications/<%= internal_pricing.medication.id %>">
              <%= internal_pricing.medication.name %>
            </a>
          <% end %>
        </td>
        <!-- Display internal_pricing.medication_id end -->

        <!-- Display internal_pricing.created_at start -->
        <td>
          <%= time_ago_in_words(internal_pricing.created_at) %> ago
        </td>
        <!-- Display internal_pricing.created_at end -->

        <!-- Display internal_pricing.updated_at start -->
        <td>
          <%= time_ago_in_words(internal_pricing.updated_at) %> ago
        </td>
        <!-- Display internal_pricing.updated_at end -->

        <td>
          <a href="/internal_pricings/<%= internal_pricing.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @internal_pricings, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
